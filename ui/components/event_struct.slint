export enum EventState {
    SigningUp,
    Active, 
    Completed,
    Cancelled,
}

export struct EventStruct {
    id: string,
    summary: string,
    summary-abbr: string,
    description: string,
    time: string,
    location: string,
    tag: string,
    larkDepartmentName: string,
    state: EventState,
    is-subscribed: bool,
    is-check-in: bool,
}
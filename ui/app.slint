import "./assets/font/MiSans-Regular.ttf";
import { Token } from "./global.slint";
import { Button } from "std-widgets.slint";

import { 
    LoginOverlay,
    LoginOverlayBridge
} from "./views/index.slint";

// export all bridge for C++ interact
export { 
    LoginOverlayBridge,
}

export component App inherits Window {
    title: "SAST Evento";
    default-font-family: "MiSans";
    min-width: 400px;
    min-height: 400px;
    preferred-width: 1024px;
    preferred-height: 634px;
    background: Token.color.surface;
    icon: Token.image.icon.evento;
    DEBUG := HorizontalLayout {
        // visible: false;
        x: 0;
        y: 0;
        height: 50px;
        Button {
            text: "< Back";
            clicked => {
            }
        }

        Button {
            text: Token.is-darkmode ? "Dark" : "Light";
            clicked => {
                Token.switch-display-mode();
            }
        }
    }

    overlay := Rectangle {
        clip: true;
        width: login.width;
        height: login.height;
        border-radius: 18px;
        border-width: 4px;
        border-color: Token.color.outline;
        login := LoginOverlay { }
    }
}
